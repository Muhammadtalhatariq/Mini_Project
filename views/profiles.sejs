<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="w-full min-h-screen bg-zinc-800 text-white p-10">
      <div class="flex justify-end w-full">
        <a class="bg-green-500 p-2 rounded-xl" href="/logout">logout</a>
      </div>
      <h1 class="text-3xl pb-4">
        hello, <span class="text-green-500"><%= user.username %></span>
      </h1>

      <h4 class="text-2xl">yor can create a new post</h4>
      <form action="/post" method="post">
        <textarea
          class="bg-transparent pt-4 outline-none w-1/3 resize-none"
          name="content"
          placeholder="what on your mind ?"
        ></textarea>
        <input
          class="bg-green-500 p-2 outline-none block rounded-xl"
          type="submit"
          value="create post"
        />
      </form>
      
      <div class="posts pt-16">
        <h3>your posts</h3>
        <div class="postContainer flex gap-4 mt-4">
          <% user.posts.reverse().forEach(post){ %>
          <div class="post mb-3 w-1/3 rounded-xl border-2 border-zinc-500 p-4">
            <h4 class="text-blue-500 mb-3"><%= user.username %></h4>
            <p class="text-sm tracking-tight"><%= post.content %></p>
            <div class="flex gap-4 mt-4">
              <a class="text-green-500" href="">like</a>
              <a class="text-blue-500" href="">edit</a>
            </div>
          </div>
          <%}%>
        </div>
      </div>
    </div>
  </body>
</html>
